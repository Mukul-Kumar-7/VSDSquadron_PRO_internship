# Data Preprocessing

Steps performed before training the model:

1. Load MNIST dataset using TensorFlow/Keras.
2. Normalize pixel values from 0–255 to 0–1.
3. Convert images to tensors.
4. Split dataset into training and testing sets.

Normalization helps the neural network train faster and improves accuracy.
